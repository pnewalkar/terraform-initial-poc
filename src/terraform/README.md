--
 |- modules
   |- main
     |- main.tf
     |- variables.tf
     |- output.tf
   |- module1
     |- main.tf
     |- variables.tf
     |- outputs.tf
 |- stacks
   |- test
     |- main.tf
     |- variables.tf
     |- outputs.tf
     |- external-data.tf
     |- provider.tf
     |- backend.tf
     |- parameters.tfvars
   |- development
     |- main.tf
     |- variables.tf
     |- outputs.tf
     |- external-data.tf
     |- provider.tf
     |- backend.tf
     |- parameters.tfvars
   